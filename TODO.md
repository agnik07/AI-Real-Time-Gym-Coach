- [ ] Explain root cause of MediaPipe ImportError (confirmed via import tests in venv).
- [ ] Propose fix: bump MediaPipe to a compatible version and/or adjust import path.
- [ ] Update requirements.txt (and optionally install) to the fixed MediaPipe version.
- [ ] Update exercise_video_processor.py imports to match the installed MediaPipe Tasks API.
- [ ] Run a quick python import test and (if possible) start Streamlit to confirm runtime works.

